# cse250a_final_project

Steps to run: 
- clone repo: `git clone https://github.com/innaamogolonova/cse250a_final_project.git`
- cd into `cse250a_final_project/src`
- run `python3 main.py`
- important to cd into `src` first before running `main.py` to avoid path configuration errors. 
- all the code is inside the `src` directory